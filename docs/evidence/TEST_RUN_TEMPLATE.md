# Протокол проверки — заполнить после выполнения

**Статус:** NOT_RUN  
**Test ID / requirement ID:** не указаны  
**Commit SHA:** не установлен  
**Дата и timezone:** не установлены  
**Исполнитель:** не указан

## Окружение

OS/архитектура; runtime/version; DB; deployment mode; чистый clone или dev environment; DATA/DEMO_AS_OF; seed checksum; policy/model/prompt version при применимости. Не указывать секретные значения.

## Предусловия

Разрешённые synthetic fixtures, initial state, role/workspace, способ доступа. Указать, использовался ли controlled provider, live API, cache или fixture.

## Выполненные команды / шаги

Только реально выполненное. Для CLI записать working directory, команду без секретов и exit code. Для UI — точные действия и начальное состояние.

## Ожидаемое поведение

Связь с требованием, точная формула/инвариант либо status/access boundary.

## Фактический результат

Не заполнен: проверка не выполнена.

## Артефакты

Пути только к реально сохранённым логам/screenshots/CSV. Пустое поле не заменять вымышленной ссылкой.

## Итог и ограничения

PASS / FAIL / NOT_RUN — выбрать на основании наблюдения. Непроверенные сценарии, выявленные дефекты, зависимость от сети/ключа, необходимость повторного прогона после исправления.
